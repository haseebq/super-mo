{
  "name": "super-mo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:quick": "node scripts/quick-test.js",
    "typecheck": "tsc --noEmit",
    "serve": "node scripts/serve.js",
    "pages:dev": "npm run build && wrangler pages dev dist --port 4173"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.88.0"
  },
  "dependencies": {
    "acorn": "^8.11.3",
    "pixi.js": "^8.14.3",
    "quickjs-emscripten": "^0.30.0",
    "ws": "^8.18.3"
  }
}
